# SAG Monitor — Environment Variables
# Copy to .env and customize for local development

# Server
DEBUG=true
PORT=8050
HOST=0.0.0.0

# Database (SQLite path for dev, PostgreSQL URL for prod)
DATABASE_URL=sag_monitor.db

# Live update interval in milliseconds (30000 = 30 seconds)
UPDATE_INTERVAL_MS=30000

# Simulation
SIMULATION_SEED=42
HISTORY_DAYS=90

# i18n
DEFAULT_LANG=es

# Alert retention
ALERT_RETENTION_DAYS=30
